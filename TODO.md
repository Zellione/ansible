# Add to core installation

sudo apt install mate-desktop-environment mate-desktop-environment-extras ubuntu-mate-themes

To run the script, you’ll need to install imagemagick and scrot. Here it is:
